{
  "name": "turnobot",
  "version": "1.0.0",
  "private": true,
  "description": "TurnoBot — plataforma SaaS de bots de WhatsApp con agenda de turnos",
  "scripts": {
    "dev": "concurrently -n server,web -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "start": "npm run start --prefix server",
    "build": "npm run build --prefix web",
    "deploy:build": "cd server && npm install && cd ../web && npm install && npm run build"
  },
  "engines": {
    "node": ">=20 <23"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
